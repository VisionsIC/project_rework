<?php
namespace App;
use Illuminate\Database\Eloquent\Model;
class DataModelHandler extends Model {
    /** This DataModelHandler Master/Super class Model handles the individual
     * sub-classes of each data model (tables) used in the ASL_RERA Database.
     */
}